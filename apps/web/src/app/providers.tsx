import { PropsWithChildren } from "react";

export default function Providers({ children }: PropsWithChildren) {
  // Ajoute ici plus tard: React Query, AuthProvider, i18n…
  return <>{children}</>;
}
