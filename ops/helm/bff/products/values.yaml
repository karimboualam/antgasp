image:
  repository: ghcr.io/your-org/antgasp-products
  tag: latest
  pullPolicy: IfNotPresent

env:
  SPRING_PROFILES_ACTIVE: "prod"
  SERVER_PORT: "8080"
  DATASOURCE_URL: "jdbc:postgresql://postgres:5432/antgasp"
  DATASOURCE_USERNAME: "postgres"
  DATASOURCE_PASSWORD: "postgres"

service:
  type: ClusterIP
  port: 8080
